version: '3.8'

services:
  pdf-server:
    build: .
    ports:
      - "4567:4567"
    environment:
      - RACK_ENV=production
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M
